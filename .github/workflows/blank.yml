name: stress-ng
on:
  push:
    branches:
      - '**'
    tags:
      - '**'
  pull_request:
    branches:
      - '**'
  release:
    types: [published, created, edited, deleted, prereleased, released]
  workflow_dispatch:

jobs:
  stress-test:
    runs-on: puzl-cloud
    steps:
    - name: Run
      run: |
        df -h
        mount
        ls -l /.puzl/
        ls -l /.puzl/pull-request-cache/
        touch /.puzl/pull-request-cache/1

